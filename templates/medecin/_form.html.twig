<div class="container">
    <div class="row">
        <div class="col-md-10 ml-md-auto">
            <div class="">
                <div class="card bg-light mb-3 mt-5" style="">
                    <div class="card-body">
                        {{ form_start(form) }}
                        {{ form_row(form.nom) }}
                        {{ form_row(form.prenom) }}
                        {{ form_row(form.adresse) }}
                        {{ form_row(form.tel) }}
                        {{ form_row(form.specialiteComplementaire) }}
                        {{ form_row(form.departement) }}

                        <button class="btn">{{ button_label|default('Sauvegarder') }}</button>

                        <a href="{{ path('medecin_index') }}"<button type="button" class="btn btn-outline-info btn-sm">Retour à la liste des médecins</a>
                            {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>








